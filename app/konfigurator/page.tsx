// → put this at:  app/konfigurator/page.tsx
// (this is exactly your current app/page.tsx — the configurator now lives at /konfigurator)
import Builder from "@/components/Builder";

export default function ConfiguratorPage() {
  return <Builder />;
}